--- FULL TOOL AUDIT — DO NOT SKIP ANY STEP AND DO NOT SKIP ANY TOOLS I Audit every tool, starting at the top of the "list of tools needing full audit.md" file whether you think you need to or not. The full list is what I want audited and documented. Start with the first tool on the list: the frontend component, the backend route file, and the latest `tools.js`. Read `tool-audit-checklist-v4_22.md.` Run a complete tool audit against every single line of the tool. No item may be skipped, deferred, or marked "out of scope." Rules: 1. Read every file in full before writing a single line of code. 2. Score every checklist item: ✅ Pass | ⚠️ Needs Work | ❌ Fail | N/A. Show the full scored list before touching any code. 3. Fix every item marked ⚠️ or ❌ — all of them, in one pass. Nothing deferred to a "future pass." 4. Items marked 🧪 (live test required) should be flagged clearly, but all 🔍 (code review) items must be fixed in code. 5. After all fixes are applied, run the full audit a second time from scratch against the fixed files. If any item still fails, fix it and audit again. Post the final audit. Do not stop until every 🔍 item scores ✅. 6. Output the final fixed files only after the second audit confirms full compliance. 7. Do not rename keys, routes, or filenames unless the audit explicitly requires it — and if you do, update every reference in the same pass. 8. If a new bug pattern is discovered during the audit that is not already documented in the checklist, add a clear warning note to the appropriate checklist section and output an updated `tool-audit-checklist-v4_xx.md` (increment the minor version) alongside the fixed tool files. Standard to meet: - `c` object uses only standard key names: `card`, `cardAlt`, `text`, `textSecondary`, `textMuted`, `input`, `btnPrimary`, `btnSecondary`, `border`, `success`, `warning`, `danger`. Additional keys allowed only for genuinely tool-specific UI states. - `card` contains background only — never bakes in border color. - `btnPrimary` / `btnSecondary` are the only button style keys — never `accent`, `tag`, `btn`, or custom names. - `border` is the single divider/border key — never `divider`. - `success`, `warning`, `danger` are combined (bg + border + text) — never split into `successBg`/`successText` etc. - Zero raw `isDark`ternaries in JSX — all extracted into named `c` keys. - Zero bespoke accent colors — all interactive elements use cyan per the family standard. - `linkStyle` defined as a standalone constant, used for every cross-tool `<a>` tag. - When running bulk find/replace: add new `c` key definitions after the sed/replace pass, never before, to prevent self-referencing TDZ errors. Verify with `grep ": c\." component.js` inside the `c = {}` block — must return zero results. Once you have finished running the audit, fix all problems you found and rerun the audit. If there are ANY discrepencies between the tool and the audit, run the audit again. Repeat this routine until the current tool is audited and no problems are found. To summarize: 1) select the next tool 2) run the audit 3)fix the problems found in the audit. 4) run the audit again. 5) repeat until there are no more errors. Only when there are no more errors, output the cleaned file and move to the next tool on the list. If you find any new bugs, document them in the latest version of "tool-audit-checklist-v4_xx.md", increment the version number and post the file to output. Always use the latest audit file. Begin. ---