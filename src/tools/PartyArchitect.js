import React, { useState, useCallback } from 'react';
import { CopyBtn } from '../components/ActionButtons';
import { useClaudeAPI } from '../hooks/useClaudeAPI';
import { useTheme } from '../hooks/useTheme';

const BRAND = '\n\n— Generated by DeftBrain · deftbrain.com';

const VIBES = [
  { value: 'chill', label: 'Chill & relaxed', emoji: '🌿' },
  { value: 'fun', label: 'Fun & lively', emoji: '🎉' },
  { value: 'elegant', label: 'Elegant', emoji: '✨' },
  { value: 'wild', label: 'Wild night', emoji: '🔥' },
  { value: 'cozy', label: 'Cozy & intimate', emoji: '🕯️' },
];

const DURATIONS = [
  { value: '2_hours', label: '~2 hrs', emoji: '⚡' },
  { value: '3_hours', label: '~3 hrs', emoji: '📖' },
  { value: '4_hours', label: '~4 hrs', emoji: '🎤' },
  { value: 'all_day', label: 'All day', emoji: '☀️' },
];

const phaseEmoji = (phase) => {
  const map = { 'Arrival': '🚪', 'Warm-up': '🌡️', 'Building': '📈', 'Peak': '🔥', 'Wind-down': '🌙', 'Exit': '👋' };
  return map[phase] || '⏱️';
};

const PartyArchitect = () => {
  const { callToolEndpoint, loading } = useClaudeAPI();
  const { theme } = useTheme();
  const isDark = theme === 'dark';

  const c = {
    pageBg:       isDark ? 'bg-[#1a1816]'    : 'bg-[#faf8f5]',
    card:         isDark ? 'bg-[#2a2623]'     : 'bg-white',
    cardAlt:      isDark ? 'bg-[#332e2a]'     : 'bg-[#faf8f5]',
    cardBorder:   isDark ? 'border-[#3d3630]' : 'border-[#e8e1d5]',
    inputBg:      isDark ? 'bg-[#1a1816]'     : 'bg-[#faf8f5]',
    inputBorder:  isDark ? 'border-[#3d3630]' : 'border-[#d5cab8]',
    inputFocus:   isDark ? 'focus:border-[#4a6a8a] focus:ring-[#2c4a6e]/20'
                         : 'focus:border-[#4a6a8a] focus:ring-[#2c4a6e]/12',
    text:          isDark ? 'text-[#f0eeea]'  : 'text-[#3d3935]',
    textSecondary: isDark ? 'text-[#c8c3b9]'  : 'text-[#5a544a]',
    textMuted:     isDark ? 'text-[#8a8275]'  : 'text-[#8a8275]',
    heading:       isDark ? 'text-[#f3efe8]'  : 'text-[#1e2a3a]',
    btnPrimary:    isDark ? 'bg-[#2c4a6e] hover:bg-[#4a6a8a] text-white' : 'bg-[#2c4a6e] hover:bg-[#1e3a58] text-white',
    btnSecondary:  isDark ? 'bg-[#332e2a] hover:bg-[#3d3630] text-[#c8c3b9] border border-[#3d3630]' : 'bg-[#f3efe8] hover:bg-[#e8e1d5] text-[#5e5042] border border-[#d5cab8]',
    btnGold:       isDark ? 'bg-[#b06d22] hover:bg-[#c8872e] text-white' : 'bg-[#c8872e] hover:bg-[#b06d22] text-white',
    badgeNeutral:  isDark ? 'bg-[#3d3630] text-[#c8c3b9]' : 'bg-[#f3efe8] text-[#73614e]',
    badgeGold:     isDark ? 'bg-[#b06d22]/20 text-[#d9a04e]' : 'bg-[#f9edd8] text-[#93541f]',
    badgePrimary:  isDark ? 'bg-[#2c4a6e]/30 text-[#a8b9ce]' : 'bg-[#d4dde8] text-[#1e3a58]',
    divider:       isDark ? 'border-[#3d3630]' : 'border-[#e8e1d5]',
    link:          isDark ? 'text-[#6e8aaa] hover:text-[#a8b9ce]' : 'text-[#2c4a6e] hover:text-[#1e3a58]',
    success:       isDark ? 'bg-[#5a8a5c]/15 border-[#5a8a5c]/30 text-[#8abf8c]' : 'bg-[#e8f0e8] border-[#5a8a5c]/20 text-[#3d6b3f]',
    warning:       isDark ? 'bg-[#c8872e]/15 border-[#c8872e]/30 text-[#d9a04e]' : 'bg-[#fdf3e4] border-[#c8872e]/20 text-[#93541f]',
    error:         isDark ? 'bg-[#b54a3f]/15 border-[#b54a3f]/30 text-[#e08a82]' : 'bg-[#fceae8] border-[#b54a3f]/20 text-[#8a3530]',
    pillActive:    isDark ? 'bg-[#2c4a6e] border-[#4a6a8a] text-white' : 'bg-[#2c4a6e] border-[#2c4a6e] text-white',
    pillInactive:  isDark ? 'bg-[#2a2623] border-[#3d3630] text-[#c8c3b9] hover:border-[#5a544a]' : 'bg-white border-[#e8e1d5] text-[#5a544a] hover:border-[#d5cab8]',
  };

  const [occasion, setOccasion] = useState('');
  const [guestCount, setGuestCount] = useState('');
  const [guestMix, setGuestMix] = useState('');
  const [space, setSpace] = useState('');
  const [budget, setBudget] = useState('');
  const [vibe, setVibe] = useState('');
  const [duration, setDuration] = useState('3_hours');
  const [constraints, setConstraints] = useState('');
  const [results, setResults] = useState(null);
  const [error, setError] = useState('');
  const [expandedSections, setExpandedSections] = useState({ timeline: true });

  const toggleSection = useCallback((key) => {
    setExpandedSections(p => ({ ...p, [key]: !p[key] }));
  }, []);

  const generate = useCallback(async () => {
    if (!occasion.trim()) return;
    setError(''); setResults(null);
    try {
      const data = await callToolEndpoint('party-architect', {
        occasion: occasion.trim(),
        guestCount: guestCount.trim() || null,
        guestMix: guestMix.trim() || null,
        space: space.trim() || null,
        budget: budget.trim() || null,
        vibe: vibe || null,
        duration: DURATIONS.find(d => d.value === duration)?.label || duration,
        constraints: constraints.trim() || null,
      });
      setResults(data);
    } catch (err) { setError(err.message || 'Failed to design event.'); }
  }, [occasion, guestCount, guestMix, space, budget, vibe, duration, constraints, callToolEndpoint]);

  const handleReset = useCallback(() => {
    setOccasion(''); setGuestCount(''); setGuestMix(''); setSpace('');
    setBudget(''); setVibe(''); setDuration('3_hours'); setConstraints('');
    setResults(null); setError('');
  }, []);

  const buildFullText = useCallback(() => {
    if (!results) return '';
    const r = results;
    let text = `🎪 PartyArchitect: ${occasion}\n`;
    if (r.energy_curve) text += `\nEnergy: ${r.energy_curve}\n`;
    if (r.timeline?.length) {
      text += '\n— Timeline —\n';
      r.timeline.forEach(t => { text += `\n${t.time} [${t.phase}]: ${t.action}\n  Host: ${t.host_note}\n`; });
    }
    if (r.mixing_strategies?.length) {
      text += '\n— Mixing Strategies —\n';
      r.mixing_strategies.forEach(m => { text += `\n${m.strategy}: ${m.how}\n`; });
    }
    if (r.food_and_drink_strategy) {
      text += `\n— Food & Drink —\n${r.food_and_drink_strategy.approach}\nTiming: ${r.food_and_drink_strategy.timing}\nSignature: ${r.food_and_drink_strategy.signature_touch}`;
    }
    if (r.the_exit) text += `\n\n🚪 Exit signal: ${r.the_exit.signal}\nSay: "${r.the_exit.script}"`;
    return text + BRAND;
  }, [results, occasion]);

  const r = results;

  return (
    <div className={`space-y-6 ${c.text}`}>
      <div className={`${c.card} border ${c.cardBorder} rounded-xl p-6`}>
        <div className={`mb-5 pb-4 border-b ${c.divider}`}>
          <h2 className={`text-2xl font-bold ${c.heading}`}>PartyArchitect 🎪</h2>
          <p className={`text-sm ${c.textSecondary} mt-1`}>Host events people actually remember</p>
        </div>

        <div className="mb-4">
          <label className={`text-sm font-bold ${c.text} block mb-1.5`}>What's the occasion?</label>
          <input type="text" value={occasion} onChange={e => setOccasion(e.target.value)}
            placeholder="e.g., housewarming, birthday dinner, game night, holiday party, team offsite"
            className={`w-full px-4 py-3 border rounded-xl text-sm ${c.inputBg} ${c.inputBorder} ${c.text} ${c.inputFocus} outline-none focus:ring-2`} />
        </div>

        <div className="grid grid-cols-2 gap-3 mb-4">
          <div>
            <label className={`text-sm font-bold ${c.text} block mb-1.5`}>Guest count</label>
            <input type="text" value={guestCount} onChange={e => setGuestCount(e.target.value)}
              placeholder="e.g., 12, 25, 50"
              className={`w-full px-4 py-3 border rounded-xl text-sm ${c.inputBg} ${c.inputBorder} ${c.text} ${c.inputFocus} outline-none focus:ring-2`} />
          </div>
          <div>
            <label className={`text-sm font-bold ${c.text} block mb-1.5`}>Budget</label>
            <input type="text" value={budget} onChange={e => setBudget(e.target.value)}
              placeholder="e.g., $200, $500, minimal"
              className={`w-full px-4 py-3 border rounded-xl text-sm ${c.inputBg} ${c.inputBorder} ${c.text} ${c.inputFocus} outline-none focus:ring-2`} />
          </div>
        </div>

        <div className="mb-4">
          <label className={`text-sm font-bold ${c.text} block mb-1.5`}>Guest mix <span className={`font-normal ${c.textMuted}`}>(key for mixing strategies)</span></label>
          <input type="text" value={guestMix} onChange={e => setGuestMix(e.target.value)}
            placeholder="e.g., work friends + college friends who don't know each other, all couples, mix of ages"
            className={`w-full px-4 py-3 border rounded-xl text-sm ${c.inputBg} ${c.inputBorder} ${c.text} ${c.inputFocus} outline-none focus:ring-2`} />
        </div>

        <div className="mb-4">
          <label className={`text-sm font-bold ${c.text} block mb-1.5`}>Space</label>
          <input type="text" value={space} onChange={e => setSpace(e.target.value)}
            placeholder="e.g., small apartment, backyard, rooftop + living room, restaurant private room"
            className={`w-full px-4 py-3 border rounded-xl text-sm ${c.inputBg} ${c.inputBorder} ${c.text} ${c.inputFocus} outline-none focus:ring-2`} />
        </div>

        <div className="mb-4">
          <label className={`text-sm font-bold ${c.text} block mb-2`}>Vibe</label>
          <div className="flex flex-wrap gap-1.5">
            {VIBES.map(v => (
              <button key={v.value} onClick={() => setVibe(vibe === v.value ? '' : v.value)}
                className={`px-3 py-1.5 rounded-lg text-xs font-bold border transition-colors min-h-[32px] ${
                  vibe === v.value ? c.pillActive : c.pillInactive}`}>
                {v.emoji} {v.label}
              </button>
            ))}
          </div>
        </div>

        <div className="mb-4">
          <label className={`text-sm font-bold ${c.text} block mb-2`}>Duration</label>
          <div className="flex gap-2">
            {DURATIONS.map(d => (
              <button key={d.value} onClick={() => setDuration(d.value)}
                className={`flex-1 py-2 rounded-xl text-[11px] font-bold border transition-colors min-h-[36px] ${
                  duration === d.value ? c.pillActive : c.pillInactive}`}>
                {d.emoji} {d.label}
              </button>
            ))}
          </div>
        </div>

        <div className="mb-5">
          <label className={`text-sm font-bold ${c.text} block mb-1.5`}>Constraints <span className={`font-normal ${c.textMuted}`}>(optional)</span></label>
          <input type="text" value={constraints} onChange={e => setConstraints(e.target.value)}
            placeholder='e.g., "vegetarian guests", "noise curfew at 10pm", "no alcohol", "kids present"'
            className={`w-full px-4 py-3 border rounded-xl text-sm ${c.inputBg} ${c.inputBorder} ${c.text} ${c.inputFocus} outline-none focus:ring-2`} />
        </div>

        <div className="flex gap-3">
          <button onClick={generate} disabled={loading || !occasion.trim()}
            className={`flex-1 ${c.btnGold} disabled:opacity-40 disabled:cursor-not-allowed font-bold py-3 px-6 rounded-xl flex items-center justify-center gap-2 min-h-[48px] shadow-lg`}>
            {loading ? <><span className="animate-spin inline-block">⏳</span> Designing your event...</> : <><span>🎪</span> Design My Event</>}
          </button>
          {results && <button onClick={handleReset} className={`px-5 py-3 ${c.btnSecondary} rounded-xl font-medium min-h-[48px]`}>New</button>}
        </div>
      </div>

      {error && (
        <div className={`${c.error} border rounded-xl p-4 flex items-start gap-3`}>
          <span className="flex-shrink-0 mt-0.5">⚠️</span><p className="text-sm">{error}</p>
        </div>
      )}

      {r && (
        <div className="space-y-4">
          <div className="flex justify-end"><CopyBtn content={buildFullText()} label="Copy All" /></div>

          {r.event_read && (
            <div className={`${c.card} border ${c.cardBorder} rounded-xl p-5`}>
              <p className={`text-sm ${c.textSecondary} leading-relaxed`}>{r.event_read}</p>
            </div>
          )}

          {r.energy_curve && (
            <div className={`${c.warning} border rounded-xl p-4`}>
              <p className="text-[10px] font-bold mb-1">📈 Energy curve</p>
              <p className="text-sm font-medium leading-relaxed">{r.energy_curve}</p>
            </div>
          )}

          {/* Timeline */}
          {r.timeline?.length > 0 && (
            <div className={`${c.card} border ${c.cardBorder} rounded-xl overflow-hidden`}>
              <button onClick={() => toggleSection('timeline')}
                className="w-full p-4 flex items-center justify-between text-left min-h-[44px]">
                <h3 className={`text-sm font-bold ${c.heading} flex items-center gap-2`}><span>⏱️</span> Event Timeline</h3>
                <span className={c.textMuted}>{expandedSections.timeline ? '▲' : '▼'}</span>
              </button>
              {expandedSections.timeline && (
                <div className={`px-4 pb-4 border-t ${c.divider} pt-3 space-y-3`}>
                  {r.timeline.map((t, i) => (
                    <div key={i} className="flex gap-3">
                      <div className="flex flex-col items-center flex-shrink-0">
                        <span className="text-lg">{phaseEmoji(t.phase)}</span>
                        {i < r.timeline.length - 1 && <div className={`w-0.5 flex-1 mt-1 ${isDark ? 'bg-[#3d3630]' : 'bg-[#e8e1d5]'}`} />}
                      </div>
                      <div className={`flex-1 ${c.cardAlt} rounded-lg p-3`}>
                        <div className="flex items-center gap-2 mb-1">
                          <span className={`text-xs font-bold ${c.heading}`}>{t.time}</span>
                          <span className={`text-[9px] font-bold px-2 py-0.5 rounded-full ${c.badgeNeutral}`}>{t.phase}</span>
                        </div>
                        <p className={`text-xs ${c.text} mb-1`}>{t.action}</p>
                        <p className={`text-[10px] ${c.textMuted} italic`}>🎯 Host: {t.host_note}</p>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          )}

          {/* Mixing strategies */}
          {r.mixing_strategies?.length > 0 && (
            <div className={`${c.card} border ${c.cardBorder} rounded-xl overflow-hidden`}>
              <button onClick={() => toggleSection('mixing')}
                className="w-full p-4 flex items-center justify-between text-left min-h-[44px]">
                <h3 className={`text-sm font-bold ${c.heading} flex items-center gap-2`}><span>🤝</span> Mixing Strategies</h3>
                <span className={c.textMuted}>{expandedSections.mixing ? '▲' : '▼'}</span>
              </button>
              {expandedSections.mixing && (
                <div className={`px-4 pb-4 border-t ${c.divider} pt-3 space-y-3`}>
                  {r.mixing_strategies.map((m, i) => (
                    <div key={i} className={`${c.cardAlt} rounded-lg p-4`}>
                      <p className={`text-xs font-bold ${c.heading} mb-1`}>{m.strategy}</p>
                      <p className={`text-xs ${c.textSecondary} mb-2`}>{m.how}</p>
                      <div className="flex gap-3">
                        <p className={`text-[10px] ${c.textMuted}`}>⏱ {m.when}</p>
                        <p className={`text-[10px] ${c.textMuted} italic`}>🧠 {m.why_it_works}</p>
                      </div>
                    </div>
                  ))}
                </div>
              )}
            </div>
          )}

          {/* Conversation starters */}
          {r.conversation_starters?.length > 0 && (
            <div className={`${c.card} border ${c.cardBorder} rounded-xl p-4`}>
              <p className={`text-xs font-bold ${c.heading} mb-2`}>💬 Conversation catalysts</p>
              <div className="space-y-1.5">
                {r.conversation_starters.map((s, i) => (
                  <p key={i} className={`text-xs ${c.textSecondary}`}>• {s}</p>
                ))}
              </div>
            </div>
          )}

          {/* Food & drink */}
          {r.food_and_drink_strategy && (
            <div className={`${c.card} border ${c.cardBorder} rounded-xl p-4`}>
              <p className={`text-xs font-bold ${c.heading} mb-2`}>🍽️ Food & drink strategy</p>
              <div className="space-y-2">
                <p className={`text-xs ${c.textSecondary}`}>{r.food_and_drink_strategy.approach}</p>
                <p className={`text-xs ${c.textSecondary}`}>⏱ {r.food_and_drink_strategy.timing}</p>
                {r.food_and_drink_strategy.signature_touch && (
                  <p className={`text-xs ${isDark ? 'text-[#d9a04e]' : 'text-[#93541f]'} font-medium`}>✨ {r.food_and_drink_strategy.signature_touch}</p>
                )}
                {r.food_and_drink_strategy.budget_option && (
                  <p className={`text-xs ${isDark ? 'text-[#8abf8c]' : 'text-[#3d6b3f]'}`}>💰 {r.food_and_drink_strategy.budget_option}</p>
                )}
              </div>
            </div>
          )}

          {/* Music plan */}
          {r.music_plan && (
            <div className={`${c.card} border ${c.cardBorder} rounded-xl p-4`}>
              <p className={`text-xs font-bold ${c.heading} mb-2`}>🎵 Music plan</p>
              <div className="space-y-1.5">
                {[['🚪 Arrival', r.music_plan.arrival], ['🔥 Peak', r.music_plan.peak], ['🌙 Wind-down', r.music_plan.wind_down]]
                  .filter(([, v]) => v).map(([label, value], i) => (
                    <p key={i} className={`text-xs ${c.textSecondary}`}>{label}: {value}</p>
                  ))}
              </div>
            </div>
          )}

          {/* The exit */}
          {r.the_exit && (
            <div className={`${c.warning} border rounded-xl p-4`}>
              <p className="text-[10px] font-bold mb-1">👋 The graceful exit</p>
              <p className={`text-xs mb-2`}>{r.the_exit.signal}</p>
              <p className={`text-xs font-bold`}>Say: "{r.the_exit.script}"</p>
            </div>
          )}

          {/* Budget */}
          {r.budget_breakdown && (
            <div className={`${c.card} border ${c.cardBorder} rounded-xl p-4`}>
              <p className={`text-xs font-bold ${c.heading} mb-2`}>💰 Budget breakdown</p>
              <div className="space-y-1.5">
                {r.budget_breakdown.total_estimate && <p className={`text-xs ${c.textSecondary}`}>Total: {r.budget_breakdown.total_estimate}</p>}
                {r.budget_breakdown.biggest_expense && <p className={`text-xs ${c.textSecondary}`}>Biggest expense: {r.budget_breakdown.biggest_expense}</p>}
                {r.budget_breakdown.where_to_save && <p className={`text-xs ${isDark ? 'text-[#8abf8c]' : 'text-[#3d6b3f]'}`}>Save on: {r.budget_breakdown.where_to_save}</p>}
                {r.budget_breakdown.free_upgrades?.length > 0 && (
                  <div className="mt-1">
                    <p className={`text-[10px] font-bold ${c.textMuted} mb-1`}>Free upgrades:</p>
                    {r.budget_breakdown.free_upgrades.map((u, i) => (
                      <p key={i} className={`text-xs ${c.textSecondary}`}>• {u}</p>
                    ))}
                  </div>
                )}
              </div>
            </div>
          )}

          {/* Disaster prevention */}
          {r.disaster_prevention?.length > 0 && (
            <div className={`${c.card} border ${c.cardBorder} rounded-xl p-4`}>
              <p className={`text-xs font-bold ${c.heading} mb-2`}>🚨 Disaster prevention</p>
              <div className="space-y-1.5">
                {r.disaster_prevention.map((d, i) => (
                  <p key={i} className={`text-xs ${c.textSecondary}`}>• {d}</p>
                ))}
              </div>
            </div>
          )}

          <div className={`${c.card} border ${c.cardBorder} rounded-xl p-4`}>
            <p className={`text-xs font-bold ${c.text} mb-2`}>Related tools</p>
            <div className="flex flex-wrap gap-2">
              <a href="/DateNight" target="_blank" rel="noopener noreferrer" className={`text-xs px-3 py-1.5 rounded-lg border ${c.pillInactive} no-underline`}>💘 DateNight</a>
              <a href="/MicroAdventureMapper" target="_blank" rel="noopener noreferrer" className={`text-xs px-3 py-1.5 rounded-lg border ${c.pillInactive} no-underline`}>🗺️ MicroAdventure Mapper</a>
              <a href="/AwkwardSilenceFiller" target="_blank" rel="noopener noreferrer" className={`text-xs px-3 py-1.5 rounded-lg border ${c.pillInactive} no-underline`}>💬 Awkward Silence Filler</a>
            </div>
          </div>
        </div>
      )}
    </div>
  );
};

PartyArchitect.displayName = 'PartyArchitect';
export default PartyArchitect;
