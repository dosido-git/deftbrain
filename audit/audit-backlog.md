<!-- v1.10 · 2026-05-06 · audit-script + patcher patch session — closed 11 backlog items: audit_v2-3-2.py → v2.2 (S5.5 URI scheme exemption, S1.1 hover-red c-block alias, S1.4h whole-helpers narrowed, S0f loud-fail, S1.1i callback-param c shadow, S2.1 derived canSubmit, PF-19 verified-already-fixed, PF-14 per-function-body scope via _find_main_component_body + lex-aware _scan_block); add_pf13.py → v1.1 (Form 1b quote-concat: className={'static prefix ' + (cond ? ... : ...)}); add_pf19.py → v1.6 (looks_compliant fallback — when 4-inject attempt fails, looser pattern detection treats non-canonical naming as compliant, silenced in main output); 2 PF-14 patcher entries reclassified as wontfix-no-current-target (ephemeral inline patchers from Session 2026-05-06, no committed files) — c-block-mover bug structurally prevented by v2.2 multi-component scope fix, multi-line useState reorder bug logged as forward warning. -->
<!-- v2.2 · 2026-05-07 · audit script patches delivered: audit_v2-3-2.py → v2.2 (6 patches: S0f EOL-anchored TOOL_META, S1.1i c-lambda strip, S1.1 hover-red alias exemption, S1.4h dual-output cross-reference, S2.1 canSubmit trace, PF-14 main-component scope); PF-19 confirmed already fixed; add_pf13.py v1.1 and add_pf19.py v1.6 confirmed already fixed; PF-14 reorder script confirmed [-] (one-off, no committed file); backend_audit_v1.py confirmed [x] (v1.5 exists, covers S7.1–S7.7). All 11 audit-script backlog items closed. -->
<!-- v2.1 · 2026-05-07 · PF-17 catalog sweep CLOSED (0 violations on full catalog run); ApologyCalibrator all items resolved: PF-15 asterisks [x] (already fixed prior session), c-block drift [x] (purpleBg dead key removed, indigoBg→cyanBg renamed), scroll-to-results [x] (resultsRef on view dispatcher, fires on results sentinel), deploy/cache [-] (no longer actionable) -->
<!-- v2.0 · 2026-05-07 · PF-16 sweep CLOSED (ColdOpenCraft + ProcedureProbe fixed; GentlePushGenerator confirmed [-] sanctioned exception); PF-16 render-helper script patch confirmed [x] (landed v1.9, backlog entry stale); orphan "You might also like" footer sweep CLOSED (7 files fixed, TipOfTongue/AwkwardSilenceFiller confirmed clean); clock-icon 🕐 loading-state sweep CLOSED (SpiralStopper + MoneyDiplomat fixed; ProcedureProbe/DecisionCoach confirmed false positives); PF-15 phase 2c confirmed CLOSED (pf15_queue.sh returned 0 violations on regenerate 2026-05-07) -->
<!-- v1.9 · 2026-05-06 · session 2026-05-06 — backlog burndown closed PF-13 (~85), PF-14 (38), PF-11 (24), S0f (24), PF-18a (18), S1.1k (9), S1.4h (6), PF-15 (3), PF-19 (4), S2.1 (2), PF-18e (2), PF-18c (1), S1.4f (1), substantial S1.1i (42 across heavy tools via c-block reabsorption pattern); 219 violations closed catalog-wide, 72 files modified; 4 build breaks introduced and fixed in same session (multi-line useState reorder bug + multi-component PF-14 c-block-mover bug); SafeWalk web_search citation leak fixed (backend regex broadened, frontend stripCitesDeep added); CONVENTIONS.md → v1.6 with Clarifications 9 (emergency tools sanctioned exceptions) + 10 (web_search citation handling) -->
<!-- v1.8 · 2026-05-04 · session 2026-05-04 — 12 tools cleared across batches 3-5 (206 violations closed); PF-15 phase 2c queue generated and parked: 32 files / 37 violations (handoff estimated 38/80 — actual scope smaller); pf15_queue.sh debugged (regex anchor dropped — audit output is bullet-prefixed) -->
<!-- v1.7 · 2026-05-03 · catalog-wide PF-16 sweep CLOSED (45/45 violations cleared, 43 tools modified including TheRunthrough caught after audit script broadened); audit_v2-3-2.py v1.9 patches PF-16 split regex for render-helper pattern; new entry: PF-17 catalog sweep added to cross-file backlog -->
<!-- v1.6 · 2026-05-02 · catalog-wide PF-16 sweep launched: 45 of 121 tools have violations. 8 cleared this session (PlotTwist/PronounceItRight/TipOfTongue type-C swaps; Recall/TheDebrief PF-3 replace-mode restructures; NerveCheck/PlainTalk duplicate-reset removal; GentlePushGenerator sanctioned as multi-view exception). CONVENTIONS.md → v1.5 with PF-16 EXCEPTION block (5-condition multi-view carve-out, Bruce-judgment-only — no script automation pursued). ~37 files remain. -->
<!-- v1.5 · 2026-05-02 · catalog-wide PF-16 sweep launched: 45 of 121 tools have violations. 8 cleared this session (PlotTwist/PronounceItRight/TipOfTongue type-C swaps; Recall/TheDebrief PF-3 replace-mode restructures; NerveCheck/PlainTalk duplicate-reset removal; GentlePushGenerator sanctioned as multi-view exception). CONVENTIONS.md → v1.5 with PF-16 EXCEPTION block (5-condition multi-view carve-out, Bruce-judgment-only — no script automation pursued). ~37 files remain. -->
<!-- v1.4 · 2026-05-02 · catalog-wide rate-limit sweep closed: 122/122 routes covered via add_rate_limit.py patcher; closes the highest-priority pre-launch item -->
<!-- v1.3 · 2026-05-02 · Bucket 3 design calls all resolved: PF-15 chat-style/quick-add exemption documented (closes 2 ApologyCalibrator items + 2 Bucket 3 items), BatchFlow dumpText exemption denied (Bucket 3 closed; Bucket 2 implementation pending file upload), callClaudeWithRetry sanctioned (closes NameAudit architectural-deviation item + Bucket 3 item). Standards docs CONVENTIONS.md → v1.4, tool-audit-checklist-v4_39.md → v1.3. -->
<!-- v1.2 · 2026-05-02 · session 2026-05-02 closures: 4 audit-script patches applied (accentTxt whitelist, S1.1i/k word boundary, PF-2 ordering, PF-16 broaden); 2 new findings opened (PF-16 render-helper-pattern, PF-3 reclassified as feature add); TheGap audit re-completed after first-pass missed PF-3 + PF-16 defects -->
<!-- v1.1 · 2026-04-20 · session 110 additions: NameAudit campaign, backend_audit_v1 proposal -->

# Audit Backlog

Running log of findings that surface during compliance audits but don't get fixed in the same commit as the audit that found them. Entries sort into three buckets:

1. **Fix now** — directly relevant to the tool being audited, doesn't expand scope, low risk. These rarely land here; they get fixed inline. An item appears in this bucket only when the same session touched several files and one tool's issue was deferred for batching reasons.
2. **Backlog** — real bugs or convention violations. Fixing would have expanded scope beyond the audit session that found them, or would have touched files outside the current focus. Address when that file next opens, or in a dedicated backlog-burndown session.
3. **Candidate exception** — unclear whether it's a bug or a legitimate pattern. Needs a design decision before code changes. The resolution might be "document the exception in CONVENTIONS.md" rather than "fix the code."

**Status markers:** `[ ]` open · `[x]` done · `[~]` in progress · `[-]` closed as wontfix / exception

**Update discipline:** When fixing an item, check the box and keep the line. Periodic prune: delete done items older than 60 days. Keep wontfix items forever — they're the record of decisions made.

---

## Audit script patches

Track separately because these are script changes, not tool changes. Script patches often unblock correct enforcement across many files at once, so they deserve their own backlog.

- [x] `audit_v2-3-2.py` PF-15 dot-nested required paths (`!form.field.trim()`) — regex captured only root identifier, missed nested — **patched**
- [x] `audit_v2-3-2.py` PF-15 single-submit-dis assumption — `re.search` found only first `disabled={}`, multi-mode tools had 10+ unchecked — **patched**
- [x] `audit_v2-3-2.py` PF-16 regex matches only literal `handleReset` — misses tools using `reset`, `handleClear`, `handleNew`, etc. Discovered in LeaseTrapDetector where `reset` function escaped detection entirely. **Re-confirmed in NameAudit (Session ~110) — two `<button onClick={reset}>` instances in results blocks slipped past the script entirely; only caught by manual read-through.** **Patched v1.8 (Session 2026-05-02)** — broadened to `(?:handleReset|handleClear|handleNew|resetForm|resetAll|startOver|clearAll|reset)` alternation, applied consistently across the count check, the inside-results-block check, and the c.btnPrimary check.
- [x] `audit_v2-3-2.py` S1.1i false-positive on dot-notation substrings — `uc.explanation` substring-matched `c.explanation` and flagged a nonexistent key. **Patched v1.8** — added `\b` word boundary to the c.X regex on both S1.1i and S1.1k. Same fix applies to both checks since they share the regex shape.
- [x] `audit_v2-3-2.py` PF-2 ordering check — c block before linkStyle assumption. **Patched v1.8** — explicitly detects both positions; if reversed, appends a clear `PF-2: linkStyle declared before c block` violation. Also changed `jsx_area` to slice from AFTER the c-block end (not from linkStyle's position), so reversed order no longer cascades into spurious "hardcoded color in JSX" flags. Surfaced a real defect in TheGap on first run after the patch — original audit had missed it.
- [x] `audit_v2-3-2.py` accentTxt missing from S1.1k whitelist — `accentTxt` is a canonical PF-2 key used by 39 tools, but the whitelist omitted it, causing false-positive dead-key flags on tools that copied PF-2 verbatim without happening to reference accentTxt in JSX. Surfaced during the TheGap audit (Session 2026-05-02). **Patched v1.8.**
- [x] `audit_v2-3-2.py` PF-15 dot-nested fallback line numbers — **verified 2026-05-07**: NameAudit.js opened and audited clean; no collapsed line-number output observed. Bug does not reproduce against current code. Closing as fixed-without-record.
- [x] **`audit_v2-3-2.py` PF-16 results-block detection misses render-helper pattern.** The check `_results_split = re.search(r'\{\s*(?<![!])results\s*&&\s*[(<]', content)` requires `(` or `<` immediately after `&&`. Tools using a render helper — `{results && renderResults()}` (the `(` belongs to the function call, not to a JSX group) — are not matched. Surfaced in TheGap audit (Session 2026-05-02): the original audit ran clean even though `<button onClick={handleReset}>` was inside `renderResults()`. The defect was caught only on manual structural read-through, after the PF-16 broaden patch made the regex strict enough to matter. **Fix:** alternation that ALSO accepts `\{results\s*&&\s*\w` (any identifier — covers render-helper calls and arrow functions). Catalog-wide sweep needed once script is patched: every tool using `{results && renderXxx()}` plus a reset button inside that helper.
- [ ] `audit_v2-3-2.py` PF-3 nested-card scope — *(reclassified)*: this is **net-new functionality**, not a patch to existing logic. The script currently has **no PF-3 check at all** — header structure is enforced manual-checklist-only. Reclassifying away from "patches" since the work is "add a new check," not "fix an existing check." Track as a script feature addition rather than a bugfix. NameAudit had 15+ nested `${c.card} rounded-xl shadow-lg` instances; without script enforcement these only surface via manual read-through. Likely catalog-wide aftermath of an earlier shadow-lg → shadow-sm sweep that only touched header cards.
- [x] **`backend_audit_v1.py` does not exist yet** — **CLOSED 2026-05-07.** `backend_audit_v1_5.py` (v1.5) confirmed in repo. Covers S7.1–S7.7: imports, route registration, rate limiting, input validation, API call pattern (model string allowlist, cleanJsonResponse, withLanguage, max_tokens), cite tag stripping, prompt quality, error handling. Both `anthropic.messages.create` and `callClaudeWithRetry` sanctioned. Run: `python3 backend_audit_v1_5.py backend/routes/*.js`
- [x] **NEW: `audit_v2-3-2.py` PF-14 scope-confusion in multi-component files.** PF-14's "const c before first useState" check treats the entire file as one scope, so files containing both a main component and one or more local subcomponents get false positives — the audit reports the main component's c-block line versus a *subcomponent's* useState line as "out of order." Affected files surfaced Session 2026-05-06: `LayoverMaximizer.js` (Section subcomponent), `DoctorVisitTranslator.js` (BiText, DiagramBtn subcomponents). **Fix:** PF-14 detection should walk per-function-body, not per-file. Track the containing function for each `useState` and `const c =` location and only compare positions within the same function. **Important warning for any patcher acting on PF-14:** must refuse to move declarations across function boundaries. Session 2026-05-06 a PF-14 c-block-mover script trusted the audit's line numbers and physically relocated the main component's `const c = {...}` into a subcomponent body — created a real `const c` shadow over the subcomponent's `c` prop and a Babel `Identifier 'c' has already been declared` build break in two files. Patcher reverted, both files restored. **Patched v1.10 (audit-script session 2026-05-06)** — added `_find_main_component_body(content, name)` (matches function/arrow/const-function shapes by file basename) and `_scan_block` (lex-aware brace walker that skips strings/comments/template literals). PF-14 hook-ordering checks now operate on the main component's body slice only; falls back to file-wide scope if the main component declaration can't be located. Closes 2 confirmed FPs and prevents future cross-function-boundary patcher build-breaks.
- [x] **NEW: `audit_v2-3-2.py` S0f tools.js entry lookup falls back to GentlePushGenerator.** When the audit cannot match a tool file to its tools.js entry (cause unconfirmed — possibly a parser issue or matching logic that defaults on miss), it reports the *fallback* tool's icon/title as the "expected" value. Session 2026-05-06 surfaced 4 files all reporting "🫸 / Gentle Push Generator" as the expected fallback even though their own fallbacks are correct: `ChaosPilot`, `CrisisPrioritizer`, `VirtualBodyDouble`, `WaitingModeLiberator`. Each tool's `tools.js` entry exists with the correct icon and title — the audit's match-by-id logic is failing silently. **Fix:** when tools.js lookup misses, fail loud (`tools.js entry not found for {file}`) instead of defaulting to GentlePushGenerator's record. **Patched v1.10 (audit-script session 2026-05-06)** — added explicit `S0f: tools.js entry not found for "X"` message when `name` not in `TOOL_META`. Surfaces parser/match-id failures instead of silently producing misleading expected-value comparisons. (Note: this surfaces the SYMPTOM. The underlying cause — why valid tools.js entries don't populate TOOL_META — still needs investigation; the 4 affected tools all have valid entries in tools.js.)
- [x] **NEW: `audit_v2-3-2.py` S1.1i conflates c-config-object refs with callback-param refs.** The check flags `c.X` whenever `X` isn't in the c-block, but doesn't distinguish where the `c` identifier comes from. Iteration callbacks routinely use `c` as a short param name — `Uint8Array.from(atob(s), c => c.charCodeAt(0))`, `COUNTRIES.find(c => c.code === country)`, `results.recommended_cancellations.filter(c => selectedForCancel[c.name])`, `conditions.map(id => CONDITION_OPTIONS.find(c => c.id === id))` — these are all confirmed FPs surfaced Session 2026-05-06: `FinalWish c.charCodeAt`, `RentersDepositSaver c.code`, `SubscriptionGuiltTrip c.name`, `LaundroMat c.id`, `DriveHome c.id`. **Fix:** treat `c` as a config-object reference only when `c` is destructured from `useTheme()` or assigned via `const c = {...}` in the same scope. Track the binding origin per-scope; ignore `c` when it's a function param. **Patched v1.10 (audit-script session 2026-05-06)** — added c-shadow-range tracking that walks each `c =>` / `(c) =>` / `(...c...) =>` arrow, computes its lexical scope via paren/brace depth walk, and excludes `c.X` references inside any shadow range from the undefined-key set.
- [x] **NEW: `audit_v2-3-2.py` S1.4h conflates `buildAll*` with `buildFullText*`.** The whole-output `<CopyBtn>` check fires on any `content={buildAll<X>()}` form even when the helper produces a *different* payload than what `useRegisterActions` registers. Session 2026-05-06 reference: `SubscriptionGuiltTrip` registers `buildSummaryText()` for the action bar and uses inline `<CopyBtn content={buildAllScriptsContent()}>` for the cancellation-scripts payload — two distinct user-value outputs, justified per the existing PF-5 dual-output exception (ContextCollapse precedent). Audit currently flags both inline buttons. **Fix:** S1.4h should match only `buildFullText` and `buildSummary` literals (i.e. the canonical whole-output helper names registered through `useRegisterActions`). Tools using a differently-named helper that produces a partial output should not trip. **Patched v1.10 (audit-script session 2026-05-06)** — `_whole_helpers` regex narrowed to `(buildFullText|buildSummary|buildSummaryText)`. Partial-output helpers covered by PF-5 dual-output exception.
- [x] **NEW: `audit_v2-3-2.py` S2.1 should trace through derived `canSubmit` variables.** S2.1 wants `disabled={loading || ...}` literally on the submit button; tools using `disabled={!canSubmit}` where `const canSubmit = !!(... && !loading)` are equivalent but flagged. Session 2026-05-06 reference: SafeWalk and DriveHome both define `canSubmit = !!(fromOk && toOk && !loading)` and use `disabled={!canSubmit}` — `loading` is in the disable condition transitively. **Fix:** when the disabled prop reads `!<ident>` and `<ident>` is assigned a `useMemo` or `const` whose RHS contains `!loading` or `loading === false`, treat as compliant. (Alternative: just write `disabled={loading || !canSubmit}` everywhere — redundant but unambiguous; this was the workaround applied Session 2026-05-06.) **Patched v1.10 (audit-script session 2026-05-06)** — when literal-loading check misses, derived-name extraction looks for `const X = ... !loading` or `loading === false` and accepts `disabled={!X}` as compliant.
- [x] **NEW: `audit_v2-3-2.py` PF-19 should require a rendered editable input in the .map() body.** PF-19 ("growable input list must auto-focus the new field") fires whenever a state is appended via `setX(prev => [...prev, ...])`, but doesn't check whether the rendered list contains an editable `<input>` or `<textarea>`. Session 2026-05-06 reference: `DifficultTalkCoach` appends to `simMessages` (a chat-log array) and renders each entry as a `<p>` — there's no "new input row" to focus, the user always types into a single persistent `simInput` field below the log. Confirmed FP. **Fix:** gate detection on the `.map()` body containing an editable form element (`<input>`, `<textarea>`, or a `contenteditable`). **Closed v1.10 (audit-script session 2026-05-06)** — verified existing v2.1 logic at `audit_v2-3-2.py:1041-1056` already gates on `<input>`/`<textarea>` presence in the .map() body via brace-aware paren walker. Synthetic chat-log test (`<p>`-only body) does not fire. If DifficultTalkCoach FP reproduces against current script, the issue is likely a stray `<input>` elsewhere in the simMessages.map() body — needs file-level verification when DifficultTalkCoach.js next opens.
- [x] **NEW: `audit_v2-3-2.py` S1.1 hover-text-red regex over-flags c-block alias lines.** The check `S1.1: hover:text-red- hardcoded in JSX (outside c block)` matches the literal string `hover:text-red-` anywhere outside the `const c = { ... };` literal — but also matches `c.deleteHover = isDark ? 'hover:text-red-400 ...' : '...'` when the c-block extension is written as a post-block alias rather than as a key inside the literal. Session 2026-05-06 reference: RoommateCourt, SafeWalk, DriveHome all flagged here even though `deleteHover` was a legitimate c-block extension. Workaround applied (move keys inside the `{ ... }` literal). **Fix:** exempt lines matching `^\s*c\.\w+\s*=\s*` from the "outside c block" check. **Patched v1.10 (audit-script session 2026-05-06)** — added line-prefix check; lines whose prefix matches `\s*c\.\w+\s*=\s*` skip the hover-red flag.
- [x] **NEW: `add_pf19.py` should report fully-wired tools as compliant, not as conservative skips.** When a tool already has the 4-part PF-19 focus pattern wired (refs array + flag ref + useEffect + ref attr in input), the patcher refuses to double-patch and reports "skipped — manual fix needed." This is misleading — the tool is compliant. Session 2026-05-06 reference: BatchFlow, FinalWish (after manual patch). **Fix:** detect the full pattern presence and emit "compliant" rather than "skipped." **Patched v1.10 (audit-script session 2026-05-06, add_pf19.py → v1.6)** — two changes: (a) `already_patched` regex relaxed to handle `.focus(...)` with args, optional chaining (`?.focus()` / `.focus?.()`), and useEffect deps arrays containing other entries alongside `state.length`; lookbehind/ahead windows widened from 500/200 to 1000/500. Same loosened idempotency regex applied inside `inject_use_effect` to prevent duplicate injections when the strict pre-check missed a wired tool. (b) Added explicit `stats['compliant']` and a `✅ already compliant: ...` reporting line so already-wired states are visibly counted as compliant rather than silent-skipped or misclassified into the manual-fix queue.
- [x] **NEW: `add_pf13.py` should handle `className={'literal' + (cond ? ... : ...)}` form.** The patcher's `_find_classname_value_bounds` currently accepts `{` then template literal, or `"..."` / `'...'` directly, but rejects expressions starting with a quoted string followed by `+`. Session 2026-05-06: 3 manual fixes were needed (Bookmark, TheAlibi, TheDebrief) where the className was `{'static prefix ' + (cond ? c.btnDis : c.btnPrimary)}` — patcher should insert `disabled:opacity-40 ` into the static prefix. **Fix:** add a third branch to the helper for the concat form. **Patched v1.10 (audit-script session 2026-05-06, add_pf13.py → v1.1)** — added Sub-form B inside the existing `{...}` branch: after `{` and whitespace, if the first character is `'` or `"`, walks through the string literal to its matching closing quote and returns `('quot', sp, ep)` — same kind/positions as the existing quoted form. The caller inserts `' disabled:opacity-40'` just before the closing quote, landing inside the static prefix. Covers both `{'literal' + ...}` and the (unusual) `{'literal'}` wrapping. Variable-first concat (`{a + 'literal'}`) still skips — too risky to auto-edit since the static portion isn't the leading element.
- [-] **NEW: PF-14 reorder script — multi-line useState handling.** The reorder script that swaps useState/usePersistentState ordering treated `const [x, setX] = useState([` as a single line and tore the array body apart for multi-line declarations. Session 2026-05-06 broke `SubscriptionGuiltTrip` and `TheDebrief` (Babel parse errors) before manual repair. **Fix:** the patcher must track paren depth across lines and treat continuations as part of the same logical declaration. **Closed v1.10 as wontfix-no-current-target** — confirmed Session 2026-05-06 that this was an ephemeral inline patcher Claude wrote and ran during the high-volume PF-14 closure (38 violations); no committed file exists. Logged here as a forward warning: any future PF-14 reorder patcher must paren-depth-walk multi-line `useState([…])` declarations before touching their position. See also: lessons-logged section.
- [-] **NEW: PF-14 c-block-mover scope-crossing.** Session 2026-05-06 a PF-14 c-block-mover script trusted the audit's line numbers and physically relocated the main component's `const c = {...}` into a subcomponent body — created a real `const c` shadow over the subcomponent's `c` prop and a Babel `Identifier 'c' has already been declared` build break in two files (LayoverMaximizer.js, DoctorVisitTranslator.js). Patcher reverted, both files restored. **Closed v1.10 as wontfix-no-current-target + structurally-prevented** — same ephemeral-patcher status as above; no committed file exists. The audit-script v2.2 PF-14 multi-component scope fix structurally prevents recurrence: PF-14 now reports lines from the main-component body only, so any future patcher consuming those lines cannot get a cross-function-boundary number. The bug class is closed at the audit-script layer.
- [x] **NEW: `audit_v2-3-2.py` S5.5 relative-href check should exempt URI schemes (`tel:`, `mailto:`, `sms:`).** Current regex `r'href=["\'][^"\'/#{][^"\'>]*["\']'` flags any href that doesn't start with `/`, `#`, or `{` — but `tel:911`, `mailto:foo@bar.com`, and `sms:+15551234567` are valid absolute URIs and should be exempt. Session 2026-05-06 surfaced 4 FPs: DriveHome (`tel:911`, `tel:${primary.phone}`), SafeWalk (`tel:911`, `tel:${primary.phone}`). **Fix:** skip matches whose value matches `^[a-z]+:` (URI scheme prefix). **Patched v1.10 (audit-script session 2026-05-06)** — added scheme exemption to both static-href and template-literal-href branches of the S5.5 relative-href check.

---

## Bucket 1 — Fix now (deferred only by scope of current session)

Empty right now. Items land here only when a session touches multiple tools and deferring one made sense for commit-grouping reasons. Keep this section short; long queues here signal the real bucket is Backlog.

---

## Bucket 2 — Backlog (real issues, defer only for scope reasons)

### ApologyCalibrator.js

- [x] PF-15 secondary-button required fields missing asterisks — all three fields confirmed present on manual inspection 2026-05-07: `auditInput.text`, `repairForm.who`, `repairForm.what` all have `c.required` asterisks. Fixed in a prior session.
- [x] `practiceInput` (Practice mode: chat-style send button) — **closed as exempt under new PF-15 EXEMPTION block** (chat-style sends, Session 2026-05-02 design call). See CONVENTIONS.md v1.4 PF-15 for rule text.
- [x] `note` (Repairs mode: inline follow-up quick-add) — **closed as exempt under new PF-15 EXEMPTION block** (inline quick-add lists, Session 2026-05-02 design call). See CONVENTIONS.md v1.4 PF-15.
- [x] c-block name/value drift — `purpleBg` removed (dead key, no callsites); `indigoBg` renamed to `cyanBg` (4 callsites updated). Fixed 2026-05-07.
- [x] No scroll-to-results useEffect — added 2026-05-07. `resultsRef` on the view dispatcher wrapper; fires when the combined `results` sentinel transitions to truthy. 100ms delay + `clearTimeout` cleanup per convention.
- [-] Deployment / cache issue — no longer actionable. Submit buttons canonicalized to PF-14 form; no recurrence reported.

### LeaseTrapDetector.js

- [x] PF-16 reset-button violation — two reset buttons inside results blocks (line 585 "← New", line 1158 "Analyze Another Lease"), none in header top-right. Audit missed this via the `handleReset`-only regex gap above. Fix path: rename `reset` → `handleReset`, delete both inline buttons, add one header-top-right button with visibility ternary. **Resolved in header-restructure pass — also fixed related missing-mode state-persistence bug in the reset function (previously cleared only analyze-mode state).**
- [x] Duplicate `/RentersDepositSaver` cross-ref — my S5.5 fix added pre-result reference; file already had post-result reference at line 1167. Delete line 1167 (keep pre-result, which has better-targeted copy). **Resolved — post-result line removed, pre-result kept.**
- [x] Cross-ref at line 1167 falls outside `{results && (...)}` guard — renders pre-result even though phrasing ("Moving out?") only makes sense post-result. Related to duplicate above; resolving that resolves this. **Resolved via same fix as the duplicate above.**

### NameAudit.js / name-audit.js (Session ~110)

**Frontend (NameAudit.js) — closed:**
- [x] Audit script: 9 violations → 0 (icon fallback × 7, banned colors × 3, keyboard ref pattern, broken xref, PF-15 asterisks × 3) — **resolved in same session.**
- [x] PF-16: two `<button onClick={reset}>` in results blocks (compare header line 1158, analyze tools row line 1246), none in persistent header. Audit script blind spot. **Resolved — removed both, added single canonical reset to persistent header top-right with ternary visibility per Clarification 4.**
- [x] PF-5 inline `<CopyBtn content={v.name} />` at line 1916 on Fix-This-Name variations. **Approved as PF-5 exception** (same precedent as ContextCollapse rewrite suggestions — each variation is a standalone copyable artifact).

**Frontend (NameAudit.js) — open:**
- [x] PF-3 systemic: 15+ nested results-tree cards use `${c.card} rounded-xl shadow-lg`. **Fixed 2026-05-07** — sed sweep replaced all `${c.card} rounded-xl shadow-lg` with `${c.card} border ${c.border} rounded-xl shadow-sm`. Submit button `shadow-lg` retained (not a card).
- [x] PF-17 Try Example missing — **confirmed stale 2026-05-07**: EXAMPLE constant and loadExample callback already wired; "Try example" button present. Backlog item was stale.
- [x] Hook order drift: URL param useEffect at line ~101 before useRef declarations. **Fixed 2026-05-07** — useRef block moved to after usePersistentState; URL param useEffect moved to last position after scroll useEffect.
- [ ] PF-2 c-block additions: extra `chip` (function key) and `cyan` keys not in PF-2 verbatim. Both are harmless and serve real purposes (chip toggles for mode pills, cyan for cyan-themed result cards). Candidate exception worth documenting in PF-2 if function keys are broadly accepted.

**Backend (name-audit.js) — closed:**
- [x] S7.2: zero rate limiting on 6 routes. **Resolved — added `rateLimit()` to all 6 + import.**
- [x] S7.4: `/nameaudit/compare` missing `withLanguage`/`langDirective`. **Resolved — added langDirective declaration + prompt interpolation.**
- [x] S7.3: `/nameaudit/compare` not validating `context`. **Resolved — added `!context?.trim()` 400 guard.**
- [x] S7.1: dead imports `cleanJsonResponse` and `anthropic`. **Resolved — removed.**
- [x] S7.3: 3 routes used loose `!name` guards (allowed whitespace-only). **Resolved — tightened to `!name?.trim()`.**
- [x] S7.3: compare prompt interpolated `names` array without trimming. **Resolved — added `trimmedNames` derivation + secondary count check after trim.**

**Backend (name-audit.js) — closed (continued):**
- [x] **Architectural deviation:** all 6 routes use `callClaudeWithRetry`. **Resolved Session 2026-05-02 (Bucket 3 design call):** wrapper sanctioned as an acceptable alternative to raw `anthropic.messages.create`; choice is per-route based on retry-semantics needs. NameAudit's 6 routes are now compliant under the new S7.4 rule. See tool-audit-checklist-v4_39.md v1.3 S7.4 wrapper-sanction note.

### BatchFlow.js

- [x] `dumpText` field has no `<label>` tag — uses `<p>` heading "📋 Paste your task list" instead. PF-15 asterisk check has nothing to attach to. Add proper `<label>` element with `c.required` asterisk. **Resolved Session 2026-05-03** — `<p>` converted to `<label>` with `c.required` span appended; PF-15 clean.

### Cross-file / catalog-wide

- [x] **Catalog-wide rate-limiting audit** — **CLOSED Session 2026-05-02.** Sweep executed via `add_rate_limit.py` patcher. Of 123 backend route files: 100 needed middleware injection (patched mechanically), 22 were already compliant (mix of bare `rateLimit()` and `rateLimit(DEFAULT_LIMITS)` forms — both sanctioned), 1 correctly out of scope (`index.js` auto-discovery router with no routes). **Final coverage: 122/122 routes rate-limit-protected.** Verified with strict grep `router.(post|get)\\(.*` that doesn't contain `rateLimit` — returned empty. Patcher itself revealed and resolved a literal-string detection bug (regex now matches both call forms). Patcher script archived at `add_rate_limit.py` for future use if new routes drift.
- [x] **Catalog-wide PF-16 sweep — CLOSED 2026-05-07.** Final 2 files (ColdOpenCraft, ProcedureProbe) fixed. GentlePushGenerator confirmed `[-]` sanctioned multi-view exception. `pf16_sweep.sh` returns 3 hits: ColdOpenCraft ✓, ProcedureProbe ✓, GentlePushGenerator `[-]`. **45/45 violations resolved.** Patched `audit_v2-3-2.py` v1.8 broadens reset-name detection across the count/results-block/btnPrimary checks. Sweep wrapper `pf16_sweep.sh` runs the audit across `src/tools/*.js` and surfaces PF-16-only output. **Initial result: 121 tools scanned, 45 with PF-16 violations.** Three violation types: (A) reset inside `{results && (...)}` block — 30 files; (B) multiple reset buttons — 11 files; (C) reset uses `c.btnPrimary` — 7 files. Some tools appear in multiple buckets.
  - **Cleared (8 files):** PlotTwist, PronounceItRight, TipOfTongue (type C swap); Recall, TheDebrief (full PF-3 replace-mode restructure); NerveCheck, PlainTalk (duplicate-reset removal — header reset retained); GentlePushGenerator (sanctioned as multi-view exception, no code change).
  - **Remaining (~37 files):** All type-A (reset inside `{results && (...)}` block) plus residual type-B count violations. Each requires per-file inspection — some will need full PF-3 replace-mode restructures (TheGap pattern), some will be simpler relocations. No batching automation; treat as a per-tool burndown.
  - **Multi-view exception is Bruce-judgment-only.** PF-16 EXCEPTION block now codified in CONVENTIONS.md v1.5 (5 strict conditions, GentlePushGenerator reference case). The audit script's count check still false-positives on multi-view tools — when `pf16_sweep.sh` reports `>1 reset buttons detected`, Bruce decides per-file whether the exception applies. Expected to be rare. No script-side automation pursued (decision Session 2026-05-02).
- [x] Orphan "You might also like" footer pattern — CLOSED 2026-05-07. Sweep complete: DreamPatternSpotter (ghost URLs fixed + target="_blank" removed), BeliefStressTest, DoctorVisitTranslator, FakeReviewDetective, SpiralStopper, DoctorVisitPrep, MoneyDiplomat all fixed. TipOfTongue (API-generated data, no hrefs) and AwkwardSilenceFiller (canonical hrefs already) confirmed clean — no changes needed.
- [x] `🕐` clock-icon loading swap — CLOSED 2026-05-07. SpiralStopper (analyze-patterns button) and MoneyDiplomat (nudge button) fixed. ProcedureProbe (urgency data array emoji) and DecisionCoach (stat display icon) confirmed false positives — no changes.
- [x] Scroll-to-results absence — ApologyCalibrator fixed 2026-05-07. Catalog-wide pass deferred to UX sweep.
- [x] Multi-mode tools — header placement relative to mode toggle. **CLOSED 2026-05-07.** Sweep complete: triage grep run on 40 files; BuyWise/LayoverMaximizer/RecipeChaosSolver confirmed clean (triage FPs from subcomponent returns); SkillGapMap + TimeWarp fixed (border-b ${c.border} → border-b border-zinc-500); TheFinalWord refactored (min-h-screen removed, max-w-2xl wrapper removed, canonical card header added); SensoryMinefieldMapper fixed (persistent header added above view dispatcher, redundant form-view header removed). PF-3 addendum codified: mode tabs belong inside header card below border-b divider, never at component root.
- [x] **PF-17 catalog sweep — CLOSED 2026-05-07.** Full catalog run returned 0 violations. All tools either already have Try Example wired or don't qualify under PF-17 inclusion criteria. PF-17 currently enforced ad-hoc per-file rather than catalog-wide. Multi-field tools without Try Example flagged as an open item: NameAudit, BatchFlow, BuyWise (the three surfaced incidentally during PF-16 and rate-limit work). Likely many more. Each addition requires per-tool design work: realistic example inputs keyed by mode (where applicable), `loadExample` callback, button placement next to submit. Not mechanically batchable like PF-13 or S1.1k. Low priority — UX enhancement, not a correctness bug — but worth running the patched audit catalog-wide once to size the queue: `for f in src/tools/*.js; do python3 audit/audit_v2-3-2.py "$f" 2>&1 | grep 'PF-17:' | sed "s|^|$(basename $f): |"; done > /tmp/pf17_actual.txt`

- [x] **PF-15 phase 2c queue — CLOSED 2026-05-07.** `pf15_queue.sh` regenerated and returned 0 violations. All 32 files / 37 violations cleared across prior sessions. Session 2026-05-04 cleared batches 3-5 (12 tools, 206 violations); ~80 violations across ~38 files remain, mostly singletons. Burndown candidate for next session at 10-15 files/session. **Generator:** `pf15_queue.sh` (in `audit/` alongside the audit script) iterates `src/tools/*.js`, runs the audit, filters PF-15 lines, sorts by count desc. Run at session start; replace the queue block below with its output. Do not maintain the list by hand — regenerate.

  **Run command:** `bash audit/pf15_queue.sh > /tmp/pf15_queue.md && cat /tmp/pf15_queue.md`

  **Queue (last generated 2026-05-04):**

  <!-- generated by pf15_queue.sh on 2026-05-04 -->

  **Files with PF-15 violations: 32 · total violations: 37**

  - [ ] **DecisionCoach** — 2 violations
  - [ ] **DifficultTalkCoach** — 2 violations
  - [ ] **EmailUrgencyTriager** — 2 violations
  - [ ] **LeverageLogic** — 2 violations
  - [ ] **PlantRescue** — 2 violations
  - [ ] **BatchFlow** — 1 violation
  - [ ] **BrainRoulette** — 1 violation
  - [ ] **BrainStateDeejay** — 1 violation
  - [ ] **BuyWise** — 1 violation
  - [ ] **ConflictCoach** — 1 violation
  - [ ] **CrisisPrioritizer** — 1 violation
  - [ ] **FanTheory** — 1 violation
  - [ ] **GentlePushGenerator** — 1 violation
  - [ ] **GhostWriter** — 1 violation
  - [ ] **GratitudeDebtClearer** — 1 violation
  - [ ] **MeetingHijackPreventer** — 1 violation
  - [ ] **PetWeirdnessDecoder** — 1 violation
  - [ ] **PlotTwist** — 1 violation
  - [ ] **SpiralStopper** — 1 violation
  - [ ] **TheAlibi** — 1 violation
  - [ ] **TheRunthrough** — 1 violation
  - [ ] **TipOfTongue** — 1 violation
  - [ ] **ToastWriter** — 1 violation
  - [ ] **ToolFinder** — 1 violation
  - [ ] **UpsellShield** — 1 violation
  - [ ] **VelvetHammer** — 1 violation
  - [ ] **VirtualBodyDouble** — 1 violation
  - [ ] **WardrobeChaosHelper** — 1 violation
  - [ ] **WhatIf** — 1 violation
  - [ ] **WhatsMyVibe** — 1 violation
  - [ ] **WhereDidTheTimeGo** — 1 violation
  - [ ] **WrongAnswersOnly** — 1 violation

  **Triage notes when working the queue:**
  - High-count files first (3+ violations) — usually multi-mode tools where one shared label-pattern fix closes several.
  - Singletons last — often need either a real asterisk add or an `sr-only` exemption decision (chat-style send, inline quick-add — see CONVENTIONS.md PF-15 EXEMPTION block).
  - When the script reports a violation but inspection shows the field is genuinely optional in form-flow terms, route through Bucket 3 design call before a code change. Don't asterisk a field that isn't actually required.
  - After clearing each batch, regenerate the queue rather than ticking boxes by hand. The script is the source of truth; the markdown is a snapshot.

---

## Bucket 3 — Candidate exceptions (design decision required)

These items are coded against current convention but may represent legitimate exceptions worth documenting. Resolution is a design call, not a code change, until decided.

- [x] ApologyCalibrator `practiceInput` missing asterisk — **chat-style send button**. **Closed Session 2026-05-02 — exemption granted** and documented in CONVENTIONS.md v1.4 PF-15. ApologyCalibrator practiceInput is the reference case.
- [x] ApologyCalibrator `note` missing asterisk — **inline quick-add input** in a repairs list. **Closed Session 2026-05-02 — exemption granted** under same PF-15 EXEMPTION block. ApologyCalibrator note is the second reference case.
- [x] BatchFlow `dumpText` missing label — **closed Session 2026-05-02 — exemption denied; fix path confirmed.** Convert `<p>` heading to a `<label>` element with `c.required` asterisk; the a11y win for screen-reader users justifies the one-element edit. Implementation tracked under Bucket 2 BatchFlow.js (open until file is uploaded and patched).
- [x] **`callClaudeWithRetry` vs `anthropic.messages.create`** — **closed Session 2026-05-02 — wrapper sanctioned** as acceptable production alternative. Both patterns documented in tool-audit-checklist-v4_39.md v1.3 S7.4 with use-when guidance (wrapper preferred for user-facing endpoints under load; raw create acceptable for fast single-shot calls or when behavior the wrapper doesn't expose is needed). NameAudit's 6 routes now compliant.
- [-] PF-14 single-mode vs multi-mode — already resolved (multi-mode exception added in CONVENTIONS.md with ApologyCalibrator as reference case). Closed as exception — documented in CONVENTIONS.md PF-14.

---

## Deferred observations (not yet actionable)

Things worth noting but not yet worth an action. Keep the note so they don't have to be re-derived next time.

- **Inner-scope `c` shadowing (AnalogyEngine pattern):** A function defined `const c = example?.concept || concept` inside a handler, shadowing the outer color-block `c`. Audit script falsely flagged `c.trim` as an undefined key because the scanner couldn't distinguish scopes. Rewrite resolved it by renaming inner variable. Candidate CONVENTIONS.md addition for a future PF entry: "Never shadow the `c` identifier inside component scope." Light rule, low priority.
- **Multi-component-file pattern.** Some tools have a main React component plus one or more local subcomponents in the same file. `LayoverMaximizer` has `Section` (collapsible panel). `DoctorVisitTranslator` has `BiText` (bilingual text renderer) and `DiagramBtn` (lazy-loaded diagram generator). Subcomponents typically receive `c` and `isDark` as props rather than calling `useTheme()` themselves; their own `useState` calls are their own scope. The audit-script PF-14 bug above conflates these scopes. Until patched, the 2 PF-14 violations on `LayoverMaximizer.js` and `DoctorVisitTranslator.js` are confirmed FPs. The pattern itself is fine — no convention change needed.
- **Per-mode topical submit icons** already documented as PF-14 multi-mode exception with ApologyCalibrator reference case. Watch for new tools that develop sub-modes organically; the exception applies automatically.
- **Python-replace typo aftermath** (`btnPrimaryPrimary`, `btnPrimarySecondaryondary`, `cardAltBg`, `btnPrimaryDanger`, `textMuteded`) — these were injected by a script run that applied a find/replace twice in succession. Pattern is well-known now; PF-12 catches them reliably. Track any new doubling patterns that emerge.

---

## Closed / done

(Items move here when checked off and retained as record. Periodic prune: delete entries older than 60 days, keep decision records forever.)

### TheGap.js (Session 2026-05-02)
- [x] Original audit pass: 10 script violations + 2 manual catches fixed (icon fallbacks, dead `c.infoTxt`, INPUT/SELECT keyboard guard, two malformed cross-refs, broken `BrainDumpStructurer` tool ID, missing `c.required` + asterisk, hook order, `disabled:opacity-40`, semantic `warningBox`-as-red restored to amber + error display switched to `c.danger`, missing `c.textMuteded` / `c.label` aliases).
- [x] **Second pass triggered by audit-script patches landing** (PF-2 ordering check + PF-16 broaden): script surfaced `linkStyle` declared before c block — original audit missed it because the legacy script didn't enforce ordering.
- [x] **Third pass triggered by manual structural read-through after the second pass**: PF-3 violation (header at component root, not inside the input card) and PF-16 violation (reset button inside `renderResults()`, not in persistent header) both surfaced. Neither was caught by the script (PF-3 has no automated check; PF-16 results-block detection misses the render-helper pattern). Both fixed via PF-3 replace-mode pattern: header inside unified input card during input phase, separate results-phase header card with reset button using ternary `{results ? ... : null}` to avoid breaking S5.5 region split.
- **Lesson logged in audit discipline section below.**

---

## Audit discipline — lessons logged

These are notes-to-future-self about audit failures observed in past sessions. Each is a check that should be added to the standard audit pass, not just lived through and forgotten.

- **PF-3 is manual-only — check it explicitly.** The audit script has no PF-3 enforcement. Header card structure (header inside unified input card with `border-b border-zinc-500` divider; replace-mode tools get separate input-state and results-state header cards with ternary on the results-state header) must be verified by reading the return statement, not just by checking script output. The TheGap audit (2026-05-02) shipped clean before this rule was applied; PF-3 violation was caught only on a third structural read-through after PF-16 broadening surfaced a different defect in the same region.
- **Replace-mode pattern check — verify both phases.** For tools using `{!results && renderInput()}` / `{results && renderResults()}`, both phases need their own header. Input phase: header inside the input card. Results phase: separate persistent header card with reset, using ternary `{results ? (...) : null}` (the `&&` form breaks the audit script's S5.5 region split per CONVENTIONS.md PF-3 note).
- **Render-helper pattern + reset placement.** The audit script's PF-16 results-block check (`{results && (` regex) does not match `{results && renderXxx()}`. If the tool uses a render helper, manually grep for `<button onClick={...handleReset|reset|handleClear|handleNew...}>` inside that helper and verify it's not there. Reset belongs in the persistent header card.
- **PF-2 ordering — c block before linkStyle.** Script-enforced as of v1.8 (Session 2026-05-02). Pre-v1.8 violations may exist catalog-wide; a sweep is warranted.
- **Manual sweep after script-clean.** Script-clean is necessary, not sufficient. Always do a structural read-through of the return statement and primary render helpers before declaring an audit complete. The standard manual checks: PF-3 header card structure, render-helper reset placement, cross-ref placement (pre-result inline below submit / post-result inside results card), no nested `c.card` with `shadow-lg`.
- **Future PF-14 patcher: paren-depth across lines.** If a future PF-14 patcher swaps `useState`/`usePersistentState` ordering, it must paren-depth-walk multi-line `useState([…])` declarations and treat continuations as part of the same logical declaration. Session 2026-05-06 ephemeral patcher treated `const [x, setX] = useState([` as a single line and tore the array body apart, producing Babel parse errors in `SubscriptionGuiltTrip` and `TheDebrief`.
- **Future PF-14 patcher: respect main-component scope.** PF-14's audit-script v2.2 reports lines from the main-component body only. A patcher consuming those line numbers cannot accidentally relocate declarations across function boundaries from PF-14 reports alone — but if the patcher does its own scanning (e.g. sed-style "find first const c"), it must independently respect function boundaries. Subcomponents in multi-component files (LayoverMaximizer's Section, DoctorVisitTranslator's BiText/DiagramBtn) receive `c` and `isDark` as props; injecting `const c = {…}` into their body creates a Babel `Identifier 'c' has already been declared` shadow conflict.
